import React from "react";
import ReactDOM from "react-dom/client";
import Pawtion from "../pawtion.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Pawtion />
  </React.StrictMode>
);
